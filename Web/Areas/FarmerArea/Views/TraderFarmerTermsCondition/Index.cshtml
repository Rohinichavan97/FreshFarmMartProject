@model IEnumerable<TraderFarmerTermsCondition>
<div class="card">
    <div class="card-header">
        <h2> TraderFarmerTermsCondition List</h2>
    </div>
    <div class="col-lg-12 stretch-card">
        <div class="card">
            <div class="card-body">
                <a asp-action="Create" class="btn btn-info"> Create New</a>
                <h4 class="card-title">Table with TraderFarmerTermsCondition List</h4>

                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Terms Condition Title</th>
                                <th scope="col">Terms Condition Desc </th>
                                <th scope="col">Actions</th>

                            </tr>
                        </thead>
                        <tbody>
                            @{
                                int srno = 1;
                            }
                            @foreach (var temp in Model)
                            {
                                <tr class="table-info">
                                    <th scope="row">@srno</th>
                                    <td>@temp.TermsConditionTitle</td>
                                    <td>@temp.TermsConditionDesc</td>
                                    <td>
                                        <a class="btn btn-warning" asp-action="Edit" asp-route-id="@temp.TraderFarmerTermsConditionID"> Edit </a>
                                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@temp.TraderFarmerTermsConditionID"> Delete </a>
                                    </td>
                                </tr>
                                srno++;
                            }

                        </tbody>
                    </table>
                </div>

            </div>
        </div>
    </div>
</div>

